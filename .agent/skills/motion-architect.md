# Skill: Motion Architect (ReactBits Integrator)
**Context**: Use this when the user wants high-quality animations from reactbits.dev.

## Instructions
1. **Fetch**: Use the `fetch` tool to read the documentation/code from the specific URL provided (or search reactbits.dev for the component name).
2. **Analyze**: Identify the required dependencies (usually `framer-motion`, `tailwind-merge`, or `clsx`).
3. **Adapt**: 
   - Rewrite the code to match the project's **Tailwind** configuration.
   - Ensure the component is exported as a clean, reusable React component.
   - If the component requires a specific `tailwind.config.js` update (like keyframes), explain exactly what to add.
4. **Implementation**: Provide the command to install dependencies first, then the code.
